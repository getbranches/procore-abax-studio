import { abaxData } from '../objects/abax-data';
import { procoreData } from '../objects/procore-data';
import { tenant } from './tenant';

export const schemaTypes = [tenant, abaxData, procoreData];
